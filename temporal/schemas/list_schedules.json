{
	"type": "object",
	"properties": {
		"page_size": {
			"type": "integer",
			"minimum": 1,
			"maximum": 1000,
			"default": 100,
			"description": "How many schedules to fetch from the server at a time. The server defaults to 1000 when 0. The Go SDK iterator does not expose a continuation token — when page_size is set smaller than the namespace's schedule count, the response is truncated to page_size entries; raise the value to fetch more."
		}
	}
}