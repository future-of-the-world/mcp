{
  "type": "object",
  "properties": {
    "workflow_id": {
      "type": "string",
      "description": "Echoes the workflow_id argument."
    },
    "run_id": {
      "type": "string",
      "description": "Run id of the workflow whose result was returned."
    },
    "result": {
      "description": "Workflow return value, decoded from the SDK's default JSON codec. Empty when the workflow is still running."
    }
  },
  "required": ["workflow_id", "run_id"]
}