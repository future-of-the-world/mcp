{
  "type": "object",
  "properties": {
    "signaled": {
      "type": "boolean",
      "description": "True if the signal was delivered to the server. The workflow itself decides whether to honor it."
    },
    "workflow_id": {
      "type": "string",
      "description": "Echoes the workflow_id argument."
    },
    "signal_name": {
      "type": "string",
      "description": "Echoes the signal_name argument."
    }
  },
  "required": ["signaled", "workflow_id", "signal_name"]
}