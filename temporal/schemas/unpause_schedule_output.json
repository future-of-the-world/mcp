{
	"type": "object",
	"properties": {
		"unpaused": {
			"type": "boolean",
			"description": "True when the Temporal server accepted the unpause. False is never returned today — failures surface as errors."
		},
		"schedule_id": {
			"type": "string",
			"description": "Echoes the schedule_id argument."
		}
	},
	"required": ["unpaused", "schedule_id"]
}