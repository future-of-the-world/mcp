{
	"type": "object",
	"properties": {
		"paused": {
			"type": "boolean",
			"description": "True when the Temporal server accepted the pause. False is never returned today — failures surface as errors."
		},
		"schedule_id": {
			"type": "string",
			"description": "Echoes the schedule_id argument."
		}
	},
	"required": ["paused", "schedule_id"]
}