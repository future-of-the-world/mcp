{
  "type": "object",
  "properties": {
    "canceled": {
      "type": "boolean",
      "description": "True if the cancel request reached the server. Canceling an already-finished workflow is a no-op success."
    },
    "workflow_id": {
      "type": "string",
      "description": "Echoes the workflow_id argument."
    },
    "run_id": {
      "type": "string",
      "description": "Echoes the run_id argument (empty string if not supplied)."
    }
  },
  "required": ["canceled", "workflow_id", "run_id"]
}