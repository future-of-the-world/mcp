{
	"type": "object",
	"properties": {
		"activity_id": {
			"type": "string",
			"description": "Standalone activity execution ID to terminate."
		},
		"run_id": {
			"type": "string",
			"description": "Optional run ID to disambiguate retries. Omit to terminate the most recent execution."
		},
		"reason": {
			"type": "string",
			"description": "Optional human-readable reason recorded on the termination event."
		}
	},
	"required": ["activity_id"]
}