{
	"type": "object",
	"properties": {
		"schedule_id": {
			"type": "string",
			"description": "Identifier of the schedule to pause. Run list_schedules first if you don't know the id."
		},
		"note": {
			"type": "string",
			"description": "Optional human-readable note attached to the pause (overwrites any existing note on the schedule)."
		}
	},
	"required": ["schedule_id"]
}