{
	"type": "object",
	"properties": {
		"url": {
			"type": "string",
			"description": "Full GitLab merge request URL, e.g. https://gitlab.example.com/group/project/-/merge_requests/42"
		},
		"page": {
			"type": "integer",
			"description": "Page number for pagination"
		},
		"per_page": {
			"type": "integer",
			"description": "Number of items per page"
		}
	},
	"required": ["url"]
}
